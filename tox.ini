[tox]
envlist = py310,py311,py312,py313
isolated_build = true

[testenv]
deps =
    pytest
    sqlalchemy>=1.4
    django>=4.2
commands =
    pytest

[testenv:lint]
deps = ruff
commands =
    ruff check .
    ruff format --check .
